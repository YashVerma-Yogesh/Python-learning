# wap to replace the double space from problem 3
name = "yash is a good boy  and is  smart"
print(name.replace("  "," "))
#  strings are immutable which means that we can not change them by running functions on them as original strint remains unchanged
print(name)